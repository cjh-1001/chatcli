# Core/local development install for chatcli.
# pyproject.toml is the source of truth for dependency versions.
#
# Install:
#   py -3 -m pip install -r requirements.txt
#
# Optional reverse-analysis Python deps:
#   py -3 -m pip install -r requirements-reverse.txt
-e .
